CMS_ENDPOINT=https://sfo.cloud.appwrite.io/v1
CMS_PROJECT_ID=6a0b4638002a71c2b8ec
CMS_DATABASE_ID=6a0b628900008b8506e3

# Create this in the Appwrite console: Project Settings -> API Keys -> Create API Key
# Scopes needed: databases.read + documents.read (read-only, no write scopes)
# (console may label the second one "Documents" or "Rows" depending on version)
# NOTE: deliberately NOT prefixed with APPWRITE_ — that prefix is reserved by
# Appwrite Sites itself and gets silently auto-injected/overridden for names
# like APPWRITE_SITE_ID (bit us on the MacWorden migration).
CMS_API_KEY=
